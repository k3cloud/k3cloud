apiVersion: v1
kind: Service
metadata:
  name: foaas-demo-svc
spec:
  selector:
    app: foaas-demo
  type: ClusterIP  
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  selector:
    app: foaas-demo
